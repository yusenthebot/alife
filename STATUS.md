# STATUS — main
updated: 2026-06-19T05:00 · loop 115
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R115 done — CHIMERA STATES (chimera.py): identical phase oscillators on a ring, symmetrically coupled via a NONLOCAL kernel + phase lag α just below π/2, spontaneously split into a COHERENT (locked) domain coexisting with an INCOHERENT (drifting) one — symmetry broken by dynamics alone (Kuramoto-Battogtokh 2002; Abrams-Strogatz). Coupling = circular convolution via FFT. Verified at κ=4,α=1.46: local order R plateau(~1)+dip(<0.5), global R~0.72 (partial), split persists in space-time, robust across 5 seeds. Controls: all-to-all coupling OR α=0 -> full sync (no chimera). Distinct from kuramoto.py + explosivesync.py. HONEST: regime narrow in (κ,α); finite-ring chimeras very long-lived (lifetime grows w/ N), not eternal. Figure: phase snapshot + local-order profile + 2 space-time kymographs. 9 tests. COMMITTED c8de76d + docs, PUSHED.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED. run.sh sets PYTHONPATH=. Push: retry on TLS (for i in 1..5; push && break || sleep 4). torch NOT installed — numpy/scipy only.
next:     R116 = FRESH divergent frontier (lean ambitious). 109 modules now — CHECK overlap via ls+grep at ORIENT (booleannet/dla/redqueen/sexevo/cellsort/kuramoto/chimera/ising(+3d)/percolation/etc already done). Candidates: COMPOSED multi-model 'world'; French-flag/positional-information morphogenesis; Turing-on-growing-domain (stripe-splitting); viscous fingering/Saffman-Taylor (have fluid.py LBM); forest-fire SOC (distinct from R74 sandpile/R107 OFC); flocking-leadership/collective-decision (if not built); Lotka-Volterra many-species/May stability-complexity; reservoir computing / echo-state (check overlap). LESSON: verify EMPIRICALLY vs known/control AND theory; ensure metric EMERGENT not circular; triangulate; PIVOT FAST ~2 attempts; CFL dt*D*4<1; chimera/narrow-regime phenomena need param probing first. NEXT REVIEW ~R120 (full-suite ~13min backstop in BACKGROUND + adversarial re-verify + gallery + tidy).
