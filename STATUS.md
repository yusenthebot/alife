# STATUS — main
updated: 2026-06-19T15:05 · loop 138
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round. CEO STEER (R123, STANDING): bias toward VISUALLY-CHECKABLE rounds.
phase:    review
owns:     all of ~/alife (single session)
doing:    R138 done — TURING ON A SPHERE (turingsphere.py): an animal coat on a curved CLOSED surface. Gray-Scott RD runs on an ICOSPHERE mesh (icosahedron subdivided n times, projected to unit sphere — no lat-lon pole singularity, near-uniform resolution). Diffusion via the row-normalized graph Laplacian (Lap u = mean(nbrs)-u, eigvals [-2,0] → standard GS step stable; conserves constants, |L·1|<1e-16). Three eyeball coat regimes by (F,k): isolated SPOTS (leopard ball, ~41 spots), LABYRINTH, CORAL. QUANT: closed geometry quantises the pattern — spot count grows with sphere size (subdiv 3/4/5 → 0/18/41 spots, ∝ area/λ²; mesh resolution = effective R/λ since normalized Laplacian sets λ in edge units). Robust seeds. HONEST: spot-count metric only meaningful in the isolated-spots regime (panel B uses that); labyrinth/coral not claimed as 'spots'; graph Laplacian approximates Laplace-Beltrami (qualitative pattern + size-scaling, not precise wavelengths). 3D render via Poly3DCollection (per-face v color) + rotating GIF. DISTINCT from coatpattern (flat 2D taper) + reactiondiff (flat GS) — closed curved manifold + topological quantization. VISUAL: 3 coat-regime balls + spot-count-vs-size + lon-lat unwrap + far-side view + 30-frame rotating GIF. 8 tests, 771 total. COMMITTED, PUSHED.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED (incl GIFs). run.sh sets PYTHONPATH=. Push: retry on git EXIT CODE (| tail masks it). torch CPU avail but unused — numpy/scipy only; imageio GIF/mp4; mpl_toolkits Poly3DCollection for 3D.
next:     R139 = FRESH VISUALLY-CHECKABLE round (CEO steer), then R140 REVIEW. 130 modules — ls+grep at ORIENT. Candidates (re-verify fresh): double-mill/vortex SPP (extend selfpropelled — counter-rotating rings, verify regime forms); dendritic solidification phase-field (Kobayashi — beautiful but stiff, PROBE first or pivot); BZ 3D scroll waves (heavy 3D iso-render); active nematics/defects (cgle has defects — check). AVOID dup: Vicsek (gpuboids), GS mitosis (reactiondiff), Eden (dielectric), Potts (cellsort+graingrowth), MIPS (mips), Physarum maze (gpuslime moderngl), Fisher/excitable (fisherfront+barkley), flat Turing (reactiondiff/coatpattern/gierermeinhardt), now sphere-Turing (turingsphere). DEFERRED (need better solver): viscous fingering, regular termite pillars, Liesegang spacing law. LESSON: icosphere = clean closed-manifold RD substrate (row-normalized Laplacian, no pole problem); match CLOSED-FORM theory or use CONTROLS; PROBE before building (icosphere blew up w/ inv=1/h² CFL → normalized Laplacian fixed it); ndimage.label returns (arr,n); np.ptp(a) not .ptp(); CFL; np.roll PERIODIC; cKDTree boxsize=L; GIF gitignored; PIVOT FAST; NEVER overclaim (禁止造假). NEXT REVIEW = R140 (full-suite backstop ALONE + adversarial re-verify R131-R139 + milestone gallery + tidy + doc refresh).
