# STATUS — main
updated: 2026-06-19T00:02 · loop 101
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R101 done + PUSHED (8b8de96) — REAL FLUID: D2Q9 LATTICE-BOLTZMANN (fluid.py), start of EVOLVED-SWIMMING arc. First real fluid dynamics. Vectorized numpy BGK (np.roll streaming, half-way bounce-back). VERIFIED vs textbook physics: Poiseuille -> parabolic profile (shape RMSE 0.005; peak within ~20% of naive analytic = velocity-shift forcing prefactor, honest); Kármán vortex street past cylinder Re=108 -> periodic shedding St=0.20 (textbook ~0.16-0.18, slightly high from channel blockage). Symmetry-break (cylinder offset + initial noise) needed to trigger shedding. Eye-verified figure (parabola + probe oscillation + vorticity vortex street). 6 tests (karman test ~16s — keep fluid tests lean), 517 total.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = project description + deploy/setup + block diagram ONLY (CEO R91, a5be5cd). Per-round progress -> progress.md; repo layout -> CODEBASE_GUIDE.md. Never re-add per-round ladder/Layout to README. run.sh sets PYTHONPATH=.
next:     R102 = IMMERSE A SELF-PROPELLING SWIMMER in the LBM fluid (alife/swimmer.py or extend fluid.py). A flexible body (linked segments / a filament) with an actuated traveling-wave gait; couple to fluid via moving bounce-back / IBM; SHOW net centre-of-mass displacement (it swims) + verify it moves only when actuated (CONTROL: no gait -> no net motion). Keep grid modest, run figure in background, cache to npz. Re-use alife/fluid.py D2Q9 (EX/EY/W/OPP, equilibrium, macroscopic). Then R103 = EVOLVE the gait (GA over amplitude/frequency/phase) to maximize swim speed (capstone). If swimmer-fluid coupling fights within ~2 attempts -> simplify (rigid flapping plate, or squirmer slip-velocity model) or PIVOT to NCA. Cold-start ORIENT. scripts: scripts/run.sh. numpy2: np.ptp(). LESSON: verify physics EMPIRICALLY vs known/control; PIVOT within ~2 attempts. NEXT REVIEW ~R110.
