# STATUS — main
updated: 2026-06-20T09:30 · loop 143
goal:     CEO DIRECTION (R140): build GENESIS — a persistent, real 3D world that, just by running (local/cloud), freely develops toward a CIVILIZATION via genuinely autonomous EVOLVED-NEURAL creatures (not scripts). Staged: 3D embodied agents → emergent language → cooperation/division-of-labour → building/economy → cumulative culture. Visually-checkable, genuine emergence, NEVER faked.
phase:    build (Stage 1 COMPLETE: R141 foundation + R142 niches + R143 arms race) → R144 = Stage 2
owns:     all of ~/alife (single session)
doing:    R143 DONE — co-evolutionary PREDATOR added (2nd evolved-neural species; prey gain predator-sense, brain n_in 9→13; n_predators0=0 byte-identical to R141/R142). REAL-VERIFY: living predator-prey ecology, boom-bust cycles, coexists 3/3 seeds; prey evasion evolves windowed +0.231 vs +0.090 frozen; 3D GIF + panel eye-verified. 800 tests. COMMITTED + PUSHED. Knife-edge tuned (pred cap 550 ~0.2x prey); arms race one-sided (R34 limit, honest).
blocked:  none
docrule:  README = description+deploy+block diagram ONLY (CEO R91); per-round catalog → progress.md; layout → CODEBASE_GUIDE.md; runs/ GITIGNORED. run.sh ulimit -v 24GB (pure-numpy alife only, NOT torch/CUDA). MEMORY: ONE sim/pytest at a time (real OOM = leaked/concurrent MuJoCo sim, not numpy); GENESIS is KD-tree + fixed-pool (bounded); close GL ctx. numpy/scipy+moderngl+imageio; torch DEFERRED CEO-gate.
next:     R144 = Stage 2 EMERGENT SIGNALLING → language from scratch. Add a cheap broadcast UTTERANCE: an evolved prey output neuron (1 extra n_out) emitted each step, and neighbours' nearest-utterance sensed as a NEW input channel — over the EXISTING kin-adjacency (R141, supplies shared interest for honest signalling, Floreano/Mitri). First scenario: food-location OR predator-alarm signalling. Additive (a flag; off = R143 world intact). ACCEPTANCE (signalling is easy to FAKE — be strict): mutual-information I(signal;world-state) > scrambled-channel baseline + a CAUSAL listening test (intervene on the message → receiver behaviour changes) + no-scripting audit (no `if food: emit`); red-team mandatory + ≥3 seeds before RECORD. Reuse alife/signals.py (Lewis signalling + mutual_information). Then Stage 3 division of labour (R142 diet specialists seed it). Architecture/roadmap in progress.md `Frontier / next`. 禁止造假.
