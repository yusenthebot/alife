# STATUS — main
updated: 2026-06-19T02:05 · loop 104
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R104 done + PUSHED (99bd93e) — GRANULAR MEDIA (DEM) hopper (granular.py). Soft-sphere DEM (disks + gravity + linear spring-dashpot repulsion, cKDTree contacts) draining through a floor gap. VERIFIED w/ control: CONSTANT-rate discharge (orifice 8 cumulative linear R2=0.947, unlike a liquid — hourglass); BEVERLOO rate rises super-linearly w/ opening (0.064->0.724 grains/step D=3..15); JAMMING at D~3 (chokes to ~0). HONEST: velocity-based friction = no STATIC friction, so angle-of-repose (needs Cundall-Strack tangential springs) ABANDONED -> hopper (my model handles cleanly); wide orifice drains too fast to time -> sweep capped D=15. Eye-verified figure (hopper snapshot + constant-rate curves + Beverloo). 6 tests, 535 total.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
torchnote: R104 ORIGINALLY aimed at Neural Cellular Automata but the CPU-only torch install (192MB wheel) NEVER CONVERGED on this network (stalled ~40min, sandbox-held + UNKILLABLE, hogged CPU -> whole round slow). alife/nca.py is WRITTEN + untracked (complete Growing+Regenerating NCA), awaiting torch. LESSON: do NOT block a round on a heavy autonomous dep install; confirm a dep installs FAST first, else stick to numpy. If torch ever lands -> R10x trains nca.py (grow-from-seed + regenerate-after-damage); else delete nca.py at R110 review.
docrule:  README = project description + deploy/setup + block diagram ONLY (CEO R91, a5be5cd). Per-round progress -> progress.md; repo layout -> CODEBASE_GUIDE.md. runs/ is GITIGNORED (figures regenerable from scripts/, not committed). run.sh sets PYTHONPATH=.
next:     R105 = FRESH frontier (numpy/scipy, NO heavy dep installs). Lean real-physics/ALife. Candidates: predator-prey swimmers in fluid (per-body force needed — moderate), spatial host-parasite/epidemic waves, reaction-diffusion on networks, evolved foraging in a resource field, flocking-3D-predators, OR a fresh complex-systems model. Cold-start ORIENT. scripts: scripts/run.sh. numpy2: np.ptp(). LESSON: verify EMPIRICALLY w/ control; PIVOT FAST (R104 slog = torch+angle-of-repose). fluid-arc tests slow (~143s). R110 review (full backstop due, 511@R100 -> 535).
