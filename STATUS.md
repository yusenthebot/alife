# STATUS — main
updated: 2026-06-20T08:10 · loop 142
goal:     CEO DIRECTION (R140): build GENESIS — a persistent, real 3D world that, just by running (local/cloud), freely develops toward a CIVILIZATION via genuinely autonomous EVOLVED-NEURAL creatures (not scripts). Staged: 3D embodied agents → emergent language → cooperation/division-of-labour → building/economy → cumulative culture. Visually-checkable, genuine emergence, NEVER faked.
phase:    build (Stage-1: foundation R141 + niches R142 shipped) → R143
owns:     all of ~/alife (single session)
doing:    R142 DONE — broke the R141 monoculture with RESOURCE PARTITIONING (n_food_types + heritable diet; each agent eats only its type = trade-off → coexisting niches, Gause). REAL-VERIFY: 3D GIF (red+green+blue diet specialists coexist) eye-verified, diet-diversity locks 3.00, directedness still evolves +0.106; red-team 3 seeds K=1 1.00 vs K=3 3.00. n_food_types=1 byte-identical to R141. 796 tests. COMMITTED + PUSHED. (Honest negative: spatial food patchiness tried first, FAILED to break monoculture.)
blocked:  none
docrule:  README = description+deploy+block diagram ONLY (CEO R91); per-round catalog → progress.md; layout → CODEBASE_GUIDE.md; runs/ GITIGNORED (GIF/panel/checkpoint local). run.sh sets ulimit -v (24GB, ALIFE_MEM_GB) — correct for pure-numpy alife, NOT torch/CUDA. numpy/scipy + moderngl(headless 3D) + imageio; torch DEFERRED CEO-gate. MEMORY: ONE sim/pytest at a time (real OOM cause = leaked/concurrent MuJoCo sim, not numpy); close GL ctx.
next:     R143 = co-evolutionary ARMS RACE. Add a PREDATOR species (evolved-neural hunters) to GENESIS so prey evolve evasion and the world stays alive/receding (prevents the Tierra plateau) — predator-mediated coexistence also helps diversity. Reuse predprey3d/coevo3d KD-tree predator lifecycle (now O(N log N) after the 2026-06-20 scale-hardening). Keep it additive (a predator flag; default off = R141/R142 world intact). ACCEPTANCE = run it, render 3D, Read GIF (hunters chasing, prey fleeing), red-team (prey survival/evasion rises vs a frozen predator, ≥3 seeds) before RECORD. Then Stage-2 = emergent signalling/language (kin-adjacency + neighbour-sense + reserved utterance hooks already in place). Architecture/roadmap in progress.md `Frontier / next`. 禁止造假.
