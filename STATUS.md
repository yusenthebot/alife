# STATUS — main
updated: 2026-06-19T04:35 · loop 113
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R113 done — 3D ISING MODEL (ising3d.py): dimensionality lifts the critical temperature. 3D analogue of R85's 2D Ising (Onsager T_c=2.269, z=4). On a cubic lattice z=6 -> order survives to T_c~4.5115 (no closed form). Vectorized 3D checkerboard Metropolis ((i+j+k) parity -> 2 sublattices, 6 neighbours all opposite colour, parallel update). T_c located THREE ways: magnetisation collapse, susceptibility peak (~4.5), size-independent Binder crossing (~4.43-4.45) -- each contrasted w/ 2D. Mean-field T_c=z (4,6) overestimates both but gets the dimension trend. Red-teamed across independent seeds (chi-peak=4.5 both times). Coordination number is the knob. Figure: |M|(T) 2D-vs-3D + chi-peak + Binder crossing + 3 mid-slices ordered/critical/disordered. 9 tests. COMMITTED 386d01d + docs, PUSHED.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED. run.sh sets PYTHONPATH=. Push: retry on TLS (for i in 1..5; push && break || sleep 4). torch NOT installed — numpy/scipy only.
next:     R114 = FRESH numpy/scipy frontier (lean ambitious). Candidates: 3D percolation p_c~0.3116 (vs 2D 0.5927) via scipy.ndimage.label in 3D — complete the dimension-dependence pair; Turing on a GROWING domain (developmental stripe-splitting — but gierermeinhardt.py/gpurd.py already cover Turing, make growth the distinct hook); host-parasite spatial coevolution waves; a COMPOSED multi-model 'world'; evolved-X; flocking-3D-predators. Cold-start ORIENT (106+ modules exist — check overlap). LESSON: verify EMPIRICALLY vs known/control AND theory; multiple independent estimators (chi-peak + Binder + |M|) = built-in red-team; finite-size shifts T_c slightly + needs honesty; PIVOT FAST within ~2 attempts; explicit-diffusion CFL dt*D*4<1. NEXT REVIEW ~R120 (full-suite backstop due, ~13min -> run in background).
