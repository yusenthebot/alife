# STATUS — main
updated: 2026-06-19T13:10 · loop 134
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round. CEO STEER (R123, STANDING): bias toward VISUALLY-CHECKABLE rounds.
phase:    review
owns:     all of ~/alife (single session)
doing:    R134 done — MURMURATION vs PREDATOR (murmuration.py): boids prey + a hawk that chases the nearest bird; a prey within sense range adds a strong FLEE force (the anti-predator ingredient on top of cohesion/align/separation). VISUAL: flock streams away from the hawk as one + evasion GIF (126 frames). CONTROL = flee weight zeroed (same flock, hawk parks in the herd and feeds). Result robust across seeds: ON vs OFF catches 80x/52x/167x fewer (seed 0/1/2); polariz 0.35; flee keeps hawk ~3.2 away vs 2.2. Honest: loose flock (not tight), no wave measured (mechanism only). cKDTree(boxsize=L) neighbours + per-agent boids loop. 7 tests (assert against random baselines: spread<L/√6, pol>2/√N), 738 total. COMMITTED 47cdb0f, PUSHED.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED (incl GIFs). run.sh sets PYTHONPATH=. Push: retry on TLS. torch NOT installed — numpy/scipy only; imageio GIF/mp4.
next:     R135 = FRESH VISUALLY-CHECKABLE round (CEO steer standing). ~125 modules — ls+grep at ORIENT. Candidates NOT built: Gray-Scott 'mitosis' self-replicating-spots GIF (reactiondiff has GS — animate dividing spots + spot-count doubling); Eden/KPZ rough-boundary growth; Faraday parametric surface waves; double-mill/vortex SPP (extend selfpropelled); Physarum maze-solving GIF (gpuslime exists); L-system fractal-plant growth GIF (lsystem exists). Deferred frontiers: viscous fingering (needs continuum solver), regular termite pillars (activator-inhibitor). LESSON: full suite ALONE; validate metric on CONTROLS (assert vs random baseline, not absolute); PROBE phenomenon forms BEFORE tests; flock metrics density+predator-disrupted → time-average over 2nd half; np.ptp(a) not .ptp(); GIF gitignored; PIVOT FAST ~2; NEVER overclaim (禁止造假). NEXT REVIEW ~R140 (full-suite backstop ALONE + adversarial re-verify R131-R139 + gallery + tidy).
