# STATUS — main
updated: 2026-06-19T04:48 · loop 114
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R114 done — SOMITOGENESIS (somitogenesis.py): clock-and-wavefront model of body segmentation (Cooke-Zeeman 1976; her1/her7 clock). Each PSM cell = a phase oscillator; a determination WAVEFRONT recedes along the axis and FREEZES each cell's phase as it passes -> a TEMPORAL rhythm becomes a periodic SPATIAL pattern of somites. Geometry forced: somite size = front speed × clock period = 2πv/ω, verified EXACTLY (max rel err 0.2%, emergent not plugged-back). Controls: omega=0 -> 1 segment; instant front -> no pattern. Posterior freq gradient -> travelling kinematic phase waves (real PSM) arresting into graded somites. Distinct from Turing/RD (gierermeinhardt/gpurd) + Kuramoto sync. Figure: wave kymograph + size-law(measured=predicted) + zebra segment strips. 9 tests. COMMITTED ad95cd1 + docs, PUSHED.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED. run.sh sets PYTHONPATH=. Push: retry on TLS (for i in 1..5; push && break || sleep 4). torch NOT installed — numpy/scipy only.
next:     R115 = FRESH divergent frontier (lean ambitious). NOTE many models exist (108 now) incl. redqueen/sexevo/sexsel/cellsort/kuramoto/epidemic/predprey(3d)/ising(+3d)/percolation/gierermeinhardt/gpurd/lsystem/morphevo — CHECK overlap via ls+grep at ORIENT. Candidates: a COMPOSED multi-model 'world' (couple existing parts -> emergent ecology); French-flag/positional-information morphogenesis; Turing-on-growing-domain; viscous fingering/Saffman-Taylor (we have LBM fluid.py); chimera states (coexisting sync+async, distinct from kuramoto); random Boolean networks (Kauffman order-chaos K=2); forest-fire SOC. LESSON: verify EMPIRICALLY vs known/control AND theory; triangulate w/ multiple independent estimators; ensure metric is EMERGENT not circular; PIVOT FAST ~2 attempts; CFL dt*D*4<1. NEXT REVIEW ~R120 (full-suite backstop ~13min -> background).
