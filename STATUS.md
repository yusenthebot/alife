# STATUS — main
updated: 2026-06-18T21:46 · loop 91
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R91 done + PUSHED (78f15df) — EVOLVED PARTICLE LIFE (evoparticle.py). GA over R61's asymmetric force matrix maximising MOTILITY (net CoM drift). Asymmetric matrix breaks Newton's 3rd law -> net momentum -> self-propulsion. TWO CONTROLS: symmetric matrices drift EXACTLY 0 (momentum conserved) vs asymmetric random mean~55/max~300; symmetrizing the EVOLVED champion kills drift 580->0 (locomotion lives in asymmetric part). Evolution amplifies ~6x (147->~700), beats random max ~2x. Eye-verified figure (motility distributions w/ symmetric-control spike at 0 + GA curve 147->692 + CoM trajectories evolved-arc/random-wander/symmetric-still + evolved swarm snapshot). GA cached to runs/r91_evoparticle/ga_result.npz for instant re-render. 6 tests, 463 total. Backstop confirmed 457@R90.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = project description + deploy/setup + block diagram ONLY (CEO R91, a5be5cd). Per-round progress -> progress.md; repo layout -> CODEBASE_GUIDE.md. Never re-add per-round ladder/Layout to README. run.sh now sets PYTHONPATH=. (works on script files).
next:     R92 — FRESH divergent BUILD round (NOT a rehash). Candidates: Turing animal-coats Gierer-Meinhardt (domain-size->spots/stripes, Murray's cheetah-tail asymmetry, FRAME vs R45 Gray-Scott), Langton's self-reproducing loop (iconic, fragile rule table-PIVOT fast), GPU megascale predator-prey, evolved swimming in a fluid, Tierra parasites (fragile), Dictyostelium aggregation (note overlaps R88). Cold-start ORIENT. scripts need PYTHONPATH=. numpy2: np.ptp(). LESSON: verify metric+control EMPIRICALLY (R91: chaser-intuition was WRONG, random already motile; symmetric control decisive); pivot within ~2 attempts. NEXT REVIEW ~R100. Heavy GA/sim rounds: run figure in background, cache results, serialize (no parallel sim agents).
