# STATUS — main
updated: 2026-06-19T09:55 · loop 128
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round. CEO STEER (R123, STANDING): bias toward VISUALLY-CHECKABLE rounds (see it work by eye, not metric plots).
phase:    review
owns:     all of ~/alife (single session)
doing:    R128 done — LANE FORMATION (lanes.py): counter-flowing crowds self-organise into lanes (Helbing pedestrians / driven colloids). Overdamped particles, periodic box, driven ±x by species + soft repulsion + noise -> same-direction walkers accrete into stripes ∥ flow. Lane order = species purity in transverse y-stripes (control-validated: lanes ~0.84, mix ~0.2); 0.1->0.89 with drive, ~0.10 with NO drive, melts above critical noise. Periodic KD-tree neighbours (fast). Diverse from PDE-pattern streak (active-matter agents); distinct from boids/mips/selfpropelled. VISUAL: mixed->lanes snapshots + order-vs-time + noise-melting + GIF. 8 tests, 704 total. COMMITTED 9862500 + docs, PUSHED.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED (incl GIFs). run.sh sets PYTHONPATH=. Push: retry on TLS. torch NOT installed — numpy/scipy only; imageio for GIF/mp4.
next:     R129 = FRESH VISUALLY-CHECKABLE round (CEO steer), then R130 = REVIEW. ~120 modules — ls+grep at ORIENT. VISUAL candidates not built: BZ Oregonator colour target+spiral waves; rendered creature-ecosystem mp4; SmoothLife (check gpulenia/conway); Chladni/Faraday nodal figures; double-mill SPP. LESSON: validate metric on CONTROLS first; PROBE phenomenon forms BEFORE building; periodic KD-tree (scipy cKDTree boxsize=L) for fast neighbour forces; blob-count+elongation over noise-prone peak counters; NEVER overclaim captions (禁止造假); np.ptp() not .ptp(); full suite ONLY at review rounds ALONE; PIVOT FAST ~2 attempts. NEXT REVIEW = R130 (after R129): full-suite backstop ALONE (~14min, runs alone!) + adversarial re-verify R120-R129 fresh seeds + milestone gallery R120-R129 + tidy + doc refresh + ambition critic.
