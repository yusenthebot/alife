# STATUS — main
updated: 2026-06-19T08:35 · loop 125
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round. CEO STEER (R123, STANDING): bias toward VISUALLY-CHECKABLE rounds (see it work by eye, not metric plots).
phase:    review
owns:     all of ~/alife (single session)
doing:    R125 done — CAHN-HILLIARD spinodal decomposition (cahnhilliard.py), Model B conserved phase separation. dc/dt=M∇²(−ε²∇²c+c³−c). Quenched mixture unmixes into ±1 domains that COARSEN as Lifshitz-Slyozov-Wagner L(t)~t^(1/3) (measured n=0.33). Conserved order parameter (mean exactly conserved) -> distinct from non-conserved RD Turing. Convex-splitting semi-implicit Fourier scheme (unconditionally stable any ε; naive scheme blows up for small ε -> need A·k² stabilizer). Length metric validated on stripe control. VISUAL: coarsening sequence + log-log L(t) + unmixing GIF. 7 tests, 681 total. COMMITTED 1d62a1e + docs, PUSHED.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED (incl GIFs). run.sh sets PYTHONPATH=. Push: retry on TLS. torch NOT installed — numpy/scipy only; imageio for GIF/mp4.
next:     R126 = FRESH VISUALLY-CHECKABLE round (CEO steer standing). ~117 modules — ls+grep at ORIENT. VISUAL candidates not built: animal coat patterns (leopard/zebra/giraffe + Murray tail-taper — most recognizable; make taper the distinct hook vs existing RD); Rayleigh-Bénard convection cells; Belousov-Zhabotinsky Oregonator colour waves (vs excitable); rendered creature-ecosystem mp4; SmoothLife continuous Lenia-like (check gpulenia). LESSON: validate metric on CONTROLS first; PROBE phenomenon+stability BEFORE building (small-ε CH naive scheme unstable -> convex-splitting stabilizer); semi-implicit/spectral for stiff 4th-order PDEs; make figure/GIF the deliverable; GIF may exceed 5MB Read limit (gitignored, static figure suffices); np.ptp() not .ptp(); full suite ONLY at review rounds ALONE; PIVOT FAST ~2 attempts. NEXT REVIEW ~R130 (4 away).
