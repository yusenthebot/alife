# STATUS — main
updated: 2026-06-19T03:30 · loop 111
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R111 done + PUSHED (18c565c) — SPATIAL ROCK-PAPER-SCISSORS + mobility (rpsmobility.py). Cyclic A>B>C>A on a lattice, reaction (predation+reproduction) + EXCHANGE mobility (conservative DOMINO-pair swaps). Distinct from R39 well-mixed RPS. Reichenbach-Mobilia-Frey: LOW mobility -> spatial cyclic domains/spirals, all 3 COEXIST (survivors 3); HIGH mobility (>~10-24) -> spirals merge -> biodiversity COLLAPSES to 1. Survival curve 3->1 across threshold. VERIFIED w/ control + conservation. HONEST: finite-size -> strong seed fluctuation near threshold (averaged collapse is clean). GOTCHA: naive vectorized swap leaks species counts -> use conservative domino swap. Eye-verified figure (coexist vs collapse snapshots + survival curve). 6 tests, 571 total.
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED. run.sh sets PYTHONPATH=. Push: retry on TLS (for i in 1..5; push && break || sleep 4). torch NOT installed — numpy/scipy only.
next:     R112 = FRESH numpy/scipy frontier (lean ambitious real-physics/ALife). Candidates: 3D extension (3D Ising T_c~4.51 / 3D percolation p_c~0.3116 dimension-dependence), Turing-on-growing-domain (stripe splitting), host-parasite spatial waves, Dictyostelium slime-mold aggregation, composed multi-model 'world', evolved-X, flocking-3D-predators. Cold-start ORIENT. scripts: scripts/run.sh. numpy2: np.ptp(). LESSON: verify EMPIRICALLY w/ control; PIVOT FAST within ~2 attempts; conservative swaps for lattice mixing. NEXT REVIEW ~R120.
