# STATUS — main
updated: 2026-06-20T05:29 · loop 140
goal:     CEO DIRECTION SHIFT (R140): build GENESIS — a persistent, real 3D world that, just by running (local/cloud), freely develops toward a CIVILIZATION, populated by genuinely autonomous EVOLVED-NEURAL creatures (not scripts). Staged: 3D embodied evolved-neural agents → emergent signalling/language → cooperation/division-of-labour → building/niche-construction/economy → cumulative culture. Visually-checkable, genuine emergence, NEVER scripted/faked.
phase:    review (done) → design/build R141
owns:     all of ~/alife (single session)
doing:    R140 REVIEW done — 781 pass alone, 17/17 headlines re-verified on FRESH seeds, gallery eye-verified, de-slop + docs (130/781) + Frontier rewritten to the GENESIS direction. COMMITTED. Next: design GENESIS substrate, build R141 foundation.
blocked:  none
docrule:  README = description+deploy+block diagram ONLY (CEO R91); per-round catalog → progress.md; layout → CODEBASE_GUIDE.md; runs/ GITIGNORED (incl GIFs). run.sh sets PYTHONPATH=. Push pre-approved each round (retry on git EXIT CODE). numpy/scipy + moderngl GPU + torch (CPU avail, RTX 5080/CUDA13 local); imageio GIF/mp4; Poly3DCollection for 3D.
next:     R141 = GENESIS FOUNDATION. Revive/scale alife/ecosim.py (R33 energy-limited living world) into a NEW persistent module (e.g. alife/genesis.py): 3D embodied agents, evolved NN brains (brain.py/neuro.py/sensors.py), metabolism + reproduction + death, behaviour evolves over a LONG run. Build ON: world3d.py, render3d.py, evolve3d/coevo3d/bigworld3d, morphevo, signals, openended/navqd (novelty/complexity instrument), GPU compute path for scale. MEMORY: run sims/pytest ALONE — never parallel (host crashed at R120 under contention); close every instance. ACCEPTANCE = run it, render 3D, Read the figure/screenshot, watch behaviour evolve, red-team the "really evolving" claim before RECORD. First DIVERGE (research SOTA artificial-civilization / open-ended-evolution / emergent-communication) → spec → TDD build → REAL-VERIFY. This is a multi-round CAMPAIGN; each round must land on runnable, demonstrated ground (never leave a half-built rewrite broken at a boundary). 禁止造假.
