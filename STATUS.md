# STATUS — main
updated: 2026-06-20T16:10 · loop 148
goal:     CEO DIRECTION (R140): build GENESIS — a persistent, real 3D world that, just by running (local/cloud), freely develops toward a CIVILIZATION via genuinely autonomous EVOLVED-NEURAL creatures (not scripts). Staged: 3D embodied agents → emergent language → cooperation/division-of-labour → building/economy → cumulative culture. Visually-checkable, genuine emergence, NEVER faked.
phase:    review — Stage-4 NICHE CONSTRUCTION ACHIEVED (R148, verified+red-teamed). Stage 5 next.
owns:     all of ~/alife (single session)
doing:    R148 DONE — Stage-4 building/niche construction = POSITIVE. building=True (requires processing) lets an
          agent deposit/reinforce a PERSISTENT hearth via the evolved process-gate (reused as a BUILD gate),
          paying build_cost. Raw food ripens ONLY near a hearth, with CONVEX reach (~3*strength, the R147
          lesson) so concentrated building pays. Hearths DECAY unless re-invested -> a standing hearth OUTLIVES
          its builder = ecological inheritance. REAL-VERIFY (2500-step hearth-coloured 3D + 3-seed controls;
          panel.png + GIF eye-verified): pop bootstraps (dips ~640 as unsettled agents starve) then recovers to
          ~2200 living almost entirely ON hearths (near_frac 0.96); inherit_ratio 6.2 (hearths 6x older than a
          lifespan), inherit_frac 1.0. PERSISTENCE PAYS: persist pop 1947 vs no-persist 1075 (1.8x), settlement
          collapses (near 0.96 -> 0.00). RED-TEAM: hearths that ripen nothing -> pop 0 (feedback is CAUSAL, not
          epiphenomenal); 100% of tall hearths maintained by builders ~17 generations LATER than their founder
          (active cross-gen inheritance, not just old structures). HONEST caveats: hearths TILE the world (pool
          saturates 600; no discrete villages, Clark-Evans R~1.26 dispersed); building is structurally
          beneficial (frozen~=evolve, NOT selection-gated). 65 genesis tests green (15 new). COMMITTED + PUSHED.
blocked:  none
docrule:  README = description+deploy+block diagram ONLY (CEO R91); per-round catalog → progress.md; layout → CODEBASE_GUIDE.md; runs/ GITIGNORED. run.sh ulimit -v 24GB (pure-numpy alife only, NOT torch/CUDA). MEMORY: ONE sim/pytest at a time; GENESIS is KD-tree + fixed-pool (bounded, <1GB); close GL ctx (r.ctx.release()). numpy/scipy+moderngl+imageio; torch DEFERRED CEO-gate.
next:     R149 = GENESIS Stage 5 — CUMULATIVE CULTURE / a complexity metric that keeps climbing, OR strengthen
          Stage 4 to DISCRETE settlements (villages). Stages: 3D foundation (R141) → diet niches (R142) →
          predator-prey arms race (R143) → [Stage 2 signalling PARKED, 2 negatives R144/R145] → Stage-3
          DIVISION OF LABOUR (R147) → Stage-4 NICHE CONSTRUCTION (R148). Stage-5 seed: a learned behaviour/skill
          that passes across generations via the built world (a cultural ratchet) → open-ended complexity. See
          progress.md ## Frontier.
