# STATUS — main
updated: 2026-06-19T04:10 · loop 112
goal:     keep going divergently until Yusen orders stop (发散思维). Fresh ALife frontiers, push each round.
phase:    review
owns:     all of ~/alife (single session)
doing:    R112 done — KELLER-SEGEL CHEMOTACTIC AGGREGATION (kellersegel.py), the 1970 Dictyostelium slime-mold model. Cells secrete a diffusing chemoattractant + crawl up its gradient -> positive feedback destabilises a uniform lawn above critical sensitivity chi_c -> collapses into mounds (chemotactic collapse). Turing-type instability driven by nonlinear ADVECTION (chi*rho*grad c), NOT reaction kinetics -> distinct from R45 Gray-Scott / R55 Physarum / R95 single-cell chemotaxis. Conservative finite-volume + UPWIND: cell mass conserved to machine precision (~1e-16), density non-negative. RIGOROUS: measured single-mode (k_min) linear growth rate lies exactly on dispersion-relation theory + crosses zero at predicted chi_c~1.03 (theory=measured). HONEST: end-state onset > chi_c (critical slowing-down). Figure: lawn->streaming->mounds + growth-rate(chi) verification. 10 tests. Pending commit+push (suite confirming).
blocked:  none. Order: keep going, never stop until ordered, divergent, push each round.
docrule:  README = description+deploy+block diagram ONLY (CEO R91). Per-round catalog -> progress.md; layout -> CODEBASE_GUIDE.md; runs/ GITIGNORED. run.sh sets PYTHONPATH=. Push: retry on TLS (for i in 1..5; push && break || sleep 4). torch NOT installed — numpy/scipy only.
next:     R113 = FRESH numpy/scipy frontier (lean ambitious). Candidates: 3D extension w/ dimension-dependence (3D Ising T_c~4.51 / 3D percolation p_c~0.3116 — coordination number sets critical point), Turing on a GROWING domain (stripe splitting — developmental), host-parasite spatial coevolution waves, a COMPOSED multi-model 'world', evolved-X, flocking-3D-predators. Cold-start ORIENT. LESSON: verify EMPIRICALLY w/ control + theory; measure linear GROWTH RATE (not finite-time end-state) near continuous transitions to dodge critical slowing-down; PIVOT FAST within ~2 attempts; watch explicit-diffusion CFL (dt*D*4<1). NEXT REVIEW ~R120 (full-suite backstop due).
